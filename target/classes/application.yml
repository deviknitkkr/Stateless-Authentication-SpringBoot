spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/stateless_authentication_springboot
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

server:
  error:
    include-message: always
    include-stacktrace: never

jwt:
  secretkey: dhuid08h048y
  token:
    title: Authorization